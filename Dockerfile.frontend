FROM node:22-bookworm-slim

WORKDIR /app

COPY package*.json ./
RUN npm install

EXPOSE 5175

# Código real vem do bind mount (docker-compose.yml) — editar no host e o
# Vite recarrega sozinho (HMR). vite.config.js fixa host:true e port:5175.
CMD ["npm", "run", "dev"]
